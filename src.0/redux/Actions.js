import { INCREMENT, DECREASE } from './actionTypes';

export const incrementAction = () => ({ type: INCREMENT, payload: 'artır' });
export const decreaseAction = () => ({ type: DECREASE, payload: 'azalt' });

